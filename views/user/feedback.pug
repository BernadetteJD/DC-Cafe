extend ../layoutUser.pug
    
block content
  div.container
    h3 Your feedback is extremely important to us. Telling us about your experience, good or bad, means that we can better understand what is important to you, what we are doing well and where there are areas where we need to make improvements.
    div.row
        div.col-sm-6.col-md-6.col-md-offset-3.col-sm-offset-3
            div.panel.panel-default
                div.panel-body
                  form(action="/users/feedback" method = 'POST')
                    div.form-group
                      input(type="text" name="firstname" class="form-control" placeholder="Your name.." required)
                    div.form-group
                      input(type="text" name="lastname" class="form-control" placeholder="Your last name.." required)
                    div.form-group
                      input(type="text" name="subject" class="form-control" placeholder="Subject" required)
                    div.form-group
                      textarea(name="description" class="form-control" placeholder="Write something.." style="height:150px" required)
                    div.form-group
                      button.btn.btn-primary.btn-sm(type="submit") Submit
    div.row
        div.col-sm-6.col-md-6.col-md-offset-3.col-sm-offset-3
           ul.list-group
                each item in feedbacks
                  div.panel.panel-default
                      li.list-group-item
                          p=`Name: ${item.firstname}`
                          p=`Subject: ${item.subject}`
                          p=item.description      